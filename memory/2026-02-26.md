## 02:11 CET — Renderowl production stabilization + admin rollout

- Implemented and shipped backend admin API enhancements in `videogen/app/api/router.py`:
  - Extended `/admin/users` response with `credits_balance`, `plan_tier`, `subscription_status`, `trial_expires_at`.
  - Updated `/admin/users/{user_id}/credits` to modify `credits_balance` (int, clamped at >=0).
  - Added new endpoint: `PATCH /admin/users/{user_id}/subscription` for superusers (tier + status updates).
- Implemented and shipped frontend admin UI at `frontend/src/app/admin/page.tsx`:
  - Lists users.
  - Adjusts credits.
  - Updates subscription tier/status.
- Enforced app-domain routing in middleware:
  - Added `/admin`, `/login`, `/signup` to app-only prefixes.
  - Kept marketing→app redirect behavior for protected paths.
- Committed and pushed to `main`:
  - `02d2d62` — `feat: enforce app subdomain paths and add admin dashboard + admin user controls`
- Triggered Coolify deploys:
  - API app UUID `mowkskw8s8wswo8cs88w04oo`.
  - Frontend app UUID `sokckc4woc0c8so80sgcs48o`.
- Verified live behavior:
  - `https://renderowl.com/dashboard` now redirects to `https://app.renderowl.com/dashboard` (no leaked `:3000` port).
  - `https://app.renderowl.com/login` returns HTTP 200.
  - API login endpoint validates normally (422 for missing username/password), indicating backend is reachable/stable.
- Noted operational detail:
  - `ADMIN_EMAIL` env currently appears empty in Coolify env listing (needs setting to intended admin email for deterministic superuser bootstrap access).
